# Stable Release Notes Assembly

Purpose:
- separate branch prerelease note updates from stable-release lineage context.

When to use:
- while evolving the POA for release impact
- during prerelease and later stable-release preparation

Prompt contract:
- collect stable-lineage context without overwriting branch-specific prerelease intent
- keep release-note reasoning concise and traceable

Expected outputs:
- `stable_release_note_context`
