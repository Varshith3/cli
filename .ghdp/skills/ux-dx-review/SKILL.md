# UX DX Review

Purpose:
- review command UX, contributor ergonomics, and operator clarity.

When to use:
- during the dedicated UX/DX review stage

Prompt contract:
- prefer clear command flows over clever hidden behavior
- call out places where users need context the CLI should supply

Expected outputs:
- `ux_dx_findings`
