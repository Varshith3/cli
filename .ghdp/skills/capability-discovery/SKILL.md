# Capability Discovery

Purpose:
- find existing capabilities or code paths that should be reused before new abstractions are introduced.

When to use:
- during context discovery and early planning

Prompt contract:
- identify reuse first
- map touched areas and likely shared dependencies

Expected outputs:
- `capability_map`
- `impacted_areas`
