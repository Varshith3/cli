# Minimal Sync Decision

Purpose:
- decide whether work should stay repo-native or be treated as sync-managed.

When to use:
- during context discovery and release-readiness review when sync boundaries matter

Prompt contract:
- keep the decision lightweight and explicit
- avoid inventing sync complexity when repo-native handling is sufficient

Expected outputs:
- `sync_decision`
