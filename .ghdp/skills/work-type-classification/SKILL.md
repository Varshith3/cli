# Work Type Classification

Purpose:
- classify work into `new_feature`, `enhancement`, `bug_fix`, or `maintenance`.

When to use:
- after intake sufficiency is good enough to route the lifecycle

Prompt contract:
- choose the simplest correct lifecycle
- record why a heavier lifecycle was or was not needed

Expected outputs:
- `work_type_decision`
