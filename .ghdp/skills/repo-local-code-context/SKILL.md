# Repo Local Code Context

Purpose:
- provide interim code/context summaries without external graph memory systems.

When to use:
- during context discovery and capability mapping

Prompt contract:
- summarize only what is needed for the current run
- keep the context reproducible from repo artifacts

Expected outputs:
- `context_summary`
