# Agent Notes (resources)

Bundled configuration shipped with the CLI (JSON manifests).

## Rules
- Prefer changing behavior by editing manifests here when possible.
- Avoid hardcoding tool lists/teams in Python if it can be expressed as data.
